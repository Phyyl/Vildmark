using OpenTK.Mathematics;
using Vildmark.Graphics.Cameras;

namespace Vildmark.Graphics.Rendering
{
    public class RenderContext2D : RenderContext
    {
        public RenderContext2D(Camera camera)
            : base(camera)
        {
        }
    }
}
